// src/services/messageService.js
const handleMessageCommand = (message) => {
  // Example: if (message.body === '!message') { message.reply('Custom message response'); }
};

export default { handleMessageCommand };
