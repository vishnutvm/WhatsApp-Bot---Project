// src/services/imageSearchService.js
const handleImageSearchCommand = (message) => {
  // Example: if (message.body.startsWith('!search image - pets')) { /* Fetch and send pet images */ }
};

export default { handleImageSearchCommand };
